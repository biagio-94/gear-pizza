class AuthEndpoints {
  // Endpoint di esempio per il login
  static const String login = '/auth/login';

  // Endpoint di esempio per la registrazione
  static const String refreshToken = '/auth/refresh';
}
